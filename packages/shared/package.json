{
  "name": "@airlab/shared",
  "version": "0.0.1",
  "directories": {
    "src": "src"
  },
  "files": [
    "lib",
    "README.md"
  ],
  "types": "./lib",
  "scripts": {
    "prebuild": "rm -rf ./lib",
    "build": "tsc",
    "prepublish": "yarn run build",
    "postinstall": "yarn run build",
    "test": "jest"
  },
  "dependencies": {
    "@nestjs/swagger": "3.1.0",
    "class-validator": "0.10.2"
  },
  "devDependencies": {
  }
}
