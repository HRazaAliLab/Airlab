{
  "name": "@airlab/shared",
  "version": "0.0.1",
  "directories": {
    "src": "src"
  },
  "files": [
    "lib",
    "README.md"
  ],
  "types": "./lib",
  "scripts": {
    "prebuild": "rm -rf ./lib",
    "build": "tsc",
    "prepublish": "yarn run build",
    "test": "jest"
  },
  "dependencies": {
    "@nestjs/swagger": "4.5.3",
    "class-validator": "0.12.1"
  },
  "devDependencies": {
  }
}
